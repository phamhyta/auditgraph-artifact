# `auditgraph/` — core auditor

*Released upon paper acceptance.* This package will contain the context graph (CPG)
builder, the path-certificate schema, the deterministic re-walk checker, the
structural-necessity (cut-edge) test, the execution-witness tier, and the recovery router.
