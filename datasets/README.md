# `datasets/` — benchmark preparation

*Released upon paper acceptance.* Normalization of the public benchmarks (SecurityEval,
CodeLMSec, CyberSecEval, and the SeCodePLT/SALLM held-out slice) into one schema, plus the
AI-code generation harness (author-model completions with the pairwise vulnerable/secure
protocol).
